"""
app/models/persistence/mongodb/
--------------------------------
MongoDB document shapes for every collection.
These mirror what is actually stored in the DB (including _id, user_id, timestamps).
"""
